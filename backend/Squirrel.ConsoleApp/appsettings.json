{
    "Logging": {
        "LogLevel": {
            "Default": "Debug",
            "System": "Information",
            "Microsoft": "Information"
        }
    },
  "WebServerSettings": {
    "BaseUrl": "http://localhost:44567"
  },
  "DbSettings": {
    "ConnectionString": "Host=localhost;Database=;Username=postgres;Password=postgres",
    // temp - for MSSQL testing we use Northwind DB from task 27
    // or Host=localhost;Database=XXXXXXX;Username=postgres;Password=postgres for PostgreSQL

    "DbType": "PostgreSql"
    //or "SqlServer"
  }
}