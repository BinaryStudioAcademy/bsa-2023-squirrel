{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    }
  },
  "WebServerSettings": {
    "BaseUrl": "http://localhost:44567"
  },
  "SignalRSettings": {
    "HubConnectionUrl": "https://localhost:7244/SquirrelHub"
  },
  "DbSettingsTmp": {
    // We will get this data from an external file on the client side
    // This settings left just for testing scripts

    "ConnectionString": "Server=localhost;Database=Northwind;Trusted_Connection=True;",
    // temp - for MSSQL testing we use Northwind DB from task 27
    // or Host=localhost;Database=YourDb;Username=postgres;Password=postgres for PostgreSQL

    "DbType": "SqlServer"
    //or "PostgreSQL"
  }
}